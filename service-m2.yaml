apiVersion: v1
kind: Service
metadata:
  name: gokafka-service-m2
  namespace: gokafka-namespace
spec:
  type: clusterIP
  selector:
    app: gokafka-service-m2
  ports:
    protocol: TCP
    port: 80
    targetPort: 8080