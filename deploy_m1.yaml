apiVersion: v1
kind: Deployment
metadata:
  name: gokakfa-deployment-m1
  namespace:  gokafka-namespace
spec:
  repilicas: "2"
  selector: 
    matchLabels:
      app: gokafka-deploy-m1
  template:
    metadata:
      labels:
        app:  gokafka-deploy-m1
    spec:
      containers:
      - name: gokafka-deploy-container-m1
        image: kafka-wyckie-microservice-2
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: "8080"
        envFrom:
        - configMapRef:
          app: gokafka-ConfigMap